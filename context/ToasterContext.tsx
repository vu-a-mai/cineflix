"use client";

// Importing necessary hooks from react-hot-toast
import { Toaster } from "react-hot-toast";

// Exporting the ToasterContext component
const ToasterContext = () => {
  return <Toaster />;
};

export default ToasterContext;
